import { ButtonhoverDirective } from './buttonhover.directive';

describe('ButtonhoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonhoverDirective();
    expect(directive).toBeTruthy();
  });
});
